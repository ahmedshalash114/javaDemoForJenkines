pipeline {

 agent any

  stages {
    stage('Scan') {
      steps {
        withSonarQubeEnv(installationName: 'sq1') {
          sh 'mvn clean install sonar:sonar -Dsonar.java.binaries=**/*.java'
        }
      }
    }
  }
}