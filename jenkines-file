pipeline {

 agent any

  stages {

   stage('Build') {
        steps {
            sh './mvnw compile'
        }
      }

    stage('Code Snippet') {
      steps {
        withSonarQubeEnv(installationName: 'sq1') {
          sh './mvnw clean install sonar:sonar'
        }
      }
    }
  }
}